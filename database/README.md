# Database

Supabase PostgreSQL migrations live in `migrations/`.

Apply locally with:

```bash
supabase db reset
```

Or paste the migration SQL into the Supabase SQL editor for an initial hosted setup.
